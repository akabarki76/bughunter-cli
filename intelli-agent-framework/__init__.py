from core.agent import AgentCore

def create_agent(config_path):
    # Placeholder for actual agent creation logic
    print(f"Creating agent with config: {config_path}")
    # For now, just return a dummy AgentCore instance
    return AgentCore(memory=None, tools=None, persona_config={})
